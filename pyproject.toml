[tool.poetry]
name = "sendmailproject"
version = "0.1.0"
description = ""
authors = ["GeeMoowy"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.13"
django = "^5.2"
python-dotenv = "^1.1.0"
psycopg2 = "^2.9.10"
redis = "^6.1.0"
pillow = "^11.2.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
